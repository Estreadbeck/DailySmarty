<% @posts.each do |post| %>
  <div><%= link_to post.title, topic_post_path(topic_id: @topic, id: post) %></div>
  <div><%= post.content %></div>
  <div><%= post.user.username %></div>
<% end %>

<%= link_to "New Post", new_post_path(topic_id: @topic.id) %>